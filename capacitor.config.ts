import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.alarabclub777dyd',
  appName: 'AlArabClub777DyD',
  webDir: 'dist'
};

export default config;
